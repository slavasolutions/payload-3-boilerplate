import { Block } from 'payload/types';

export const sample-cardSchema: Block = {
  slug: 'sample-card',
  labels: {
    singular: 'SampleCard',
    plural: 'SampleCards',
  },
  fields: [
    
  ],
};
